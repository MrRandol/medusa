export interface Media {
    id: Int16Array;
    fileName: string;
    filePath: string;
    width: number;
    height: number;
}

export interface Dimension {
    w: number;
    h: number;
}